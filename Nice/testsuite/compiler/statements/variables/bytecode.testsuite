/// PASS
  assert foo([1.0,2.0], true) == 1.0;
  /// Toplevel
<T | T <: double, int <: T >
T foo(T[] a, boolean first) 
{
  T res;
  if (first)
    res = a[0];
  else
    res = a[1];
  return res;
}

/// PASS
  ?String foo = null;
  assert foo == null;
