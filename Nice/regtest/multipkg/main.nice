package regtest.multipkg;

import regtest.multipkg.a;
import regtest.multipkg.b;

class AA extends A {}
class BB extends B {}

main(args)
{
  println(f(new A()) + " " + 
	  f(new B()) + " " + 
	  f(new AA()) + " " + 
	  f(new BB()));
}
