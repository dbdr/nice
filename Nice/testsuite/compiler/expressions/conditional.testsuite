/// PASS
  int i = 0;
  boolean closed = false;
  i = closed 
    ? (i >= 66 ? 0 : -1000)
    : i - i;

/// FAIL
  /// Toplevel
  int nameX() = 0;
  var int nameX = 1;
  int dummy() = false ? 0 : nameX;
