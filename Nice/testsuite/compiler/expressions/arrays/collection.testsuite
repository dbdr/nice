/// PASS
  assert map(["A"], String s => s+s)[0].equals("AA");

/// PASS
  len([ "" ]);
  /// Toplevel
<T, Collection C> C<T> len(C<T>);
len(Collection c) = c;
len(Array c) { int i = c.length; return c; }

