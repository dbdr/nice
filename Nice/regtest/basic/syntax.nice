package regtest.basic;

// Some bizare syntactic constructs that should be accepted

void syntaxTest()
{
  // While with empty body
  while(false)
    ;

  // If with empty body
  if (true)
    ;

  // [] is the empty array
  if ([] == null)
    throw new Error();
}
