package regtest.basic;

class Parameterized<T>
{
  T x;

  void setX_fun(T value)
  {
    T tmp = value;
    this.x = tmp;
  }

  void setX_meth(T value);
  <T> setX_meth(value)
  {
    T tmp = value;
    this.x = tmp;
  }
}

