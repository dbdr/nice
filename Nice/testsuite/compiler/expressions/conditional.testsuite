/// PASS
  int i = 0;
  boolean closed = false;
  i = closed 
    ? (i >= 66 ? 0 : -1000)
    : i - i;

/// FAIL
  /// Toplevel
  int nameX() = 0;
  byte nameX() = 1;
  int dummy() = false ? 0 : nameX;

/// PASS
  int i = 0;
  void f() { if (true) i++; }
  f();
  assert i == 1;
